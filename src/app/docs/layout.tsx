export const metadata = {
    title: "API Documentation",
    description: "Swagger UI for Appointment Booking System API",
};

export default function DocsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
